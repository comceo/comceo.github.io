name: Update README with MD Files

on:
  push:
    paths:
      - "*.md"
    branches:
      - main

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 确保工作流有写入仓库内容的权限

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Update README
        run: |
          # 获取所有 .md 文件（排除 README.md），并按修改时间排序（最新的排在最前面）
          md_files=$(ls -t *.md | grep -v "README.md")

          # 确保 README.md 有写权限
          chmod +w README.md

          # 生成新的 README 内容
          echo "# My Blog" > README.md
          echo "" >> README.md
          echo "## Articles" >> README.md
          echo "" >> README.md

          # 为每个 .md 文件生成链接
          for file in $md_files; do
            echo "- [${file%.md}]($file)" >> README.md
          done

      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "Update README with MD files"
          branch: main
